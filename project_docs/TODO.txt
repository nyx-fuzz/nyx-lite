PRE RELEASE:
* proper memory api
  * Process Memory Abstraction
  * Api that returns shared pages to process memory
  * Allow to not reset shared pages
  * Add testing
* incremental snapshots
* snapshot tree api
* timeout on execution
* api to load dockerfiles directly
* api to load a rootdir based fs directly
* E2E testing setup
* documentation
  * a minimal fuzzing example
  * cleaning up lint errors
  * comments on the actual api

* harddisk snapshots & isolation
* root snapshot sharing between processes
* profiling snapshot performance
* breakpoints
* singlestepping
* investigate syncing emulation to snapshots
* investigate nested virtualization
